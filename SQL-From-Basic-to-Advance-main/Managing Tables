1. CREATE TABLE T (
      id INT PRIMARY KEY,
      name VARCHAR NOT NULL,
      price INT DEFAULT 0
   );
=> Create a new table with three column.

2. DROP TABLE t;
=> Drop the whole table from the Database.

3. ALTER TABLE t ADD column;
=> Add a new column in table t.

4. ALTER TABLE t DROP column c;
=> Drop column c from the table.

5. ALTER TABLE t ADD/ DROP CONSTRAINT;
=> Add or Drop a Constraint.

6. ALTER TABLE t1 REMANE TO t2;
=> Rename a table from t1 to t2.

7. ALTER TABLE t1 RENAME C1 TO C2;
=> Rename column C1 to C2

8. TRUNCATE TABLE t;
=> Remove all data from the table.

9. DELETE FROM Customers WHERE CustomerName='Alfreds Futterkiste';
=> Delete is used to remove a particular record from the table.
